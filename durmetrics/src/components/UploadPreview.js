import React from 'react';

const UploadPreview = (props) => {
        return (
                <div className="upload-panel">
                        <div className="upload-title">Data preview</div>
                        <div className="panel-bar"></div>
                </div>
        );
};

export default UploadPreview